import React from 'react'
import { ProfileDetailProvider } from '@daroox'

const Profile = (): JSX.Element => {

    return (
        <div>
            <ProfileDetailProvider />
        </div>
    )
}

export default Profile
