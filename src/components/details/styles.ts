/*
  Components - Details - Styles
*/

import { Container } from '@mui/material'
import styled from 'styled-components'

export const StyledContainer = styled(Container)`
  border: 1px dashed red;
  .list {
    width: 100vh;
    border: 1px dashed green;
  }
`
